<%= form_for @micropost do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :content %>
  </div>
  
  <div class="actions">
 <!-- > <p style ="float:right"> --> <%= f.submit "Submit" %> <!-- </p> -->
  </div>
<% end %>

